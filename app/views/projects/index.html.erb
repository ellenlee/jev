<h1>JEV</h1>

<table class="table">
	<tr>
		<th>#</th>
		<th>專案名稱</th>
		<th>狀態</th>
		<th>工作階段</th>
		<th>執行班級</th>
	</tr>
	<% (Status.find(2).projects + Status.find(1).projects).each do |project| %>
	<tr>
			<td><%= project.id %></td>
			<td><%= link_to project.name, project_stages_path(project) %></td>
			<td><%= project.status.name %></td>	
			<td>
				<% project.stages.each do |s| %>
					<%= s.name %><br>
				<% end %>	
			</td>
			<td>
				
			</td>
	</tr>	
	<% end %>
</table>
