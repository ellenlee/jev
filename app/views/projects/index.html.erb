<h1>JEV</h1>

<table class="table">
	<tr>
		<th>#</th>
		<th>專案名稱</th>
		<th>工作階段</th>
		<th>執行班級</th>
	</tr>
	<% @projects.each do |project| %>
	<tr>
			<td><%= project.id %></td>
			<td><%= link_to project.name, project_stages_path(project) %></td>
			<td>
				<% project.stages.each do |s| %>
					<%= s.name %><br>
				<% end %>	
			</td>
			<td>
				
			</td>
	</tr>	
	<% end %>
</table>
